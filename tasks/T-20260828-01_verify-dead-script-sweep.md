# T-20260828-01 横断調査「不要スクリプト」所見の再検証（Jishushitsu）

- status: ready            <!-- ready / in-progress / done / returned -->
- issuer: opus-5（Roasting-HACCP セッション / Ocean型 横断調査）
- executor: sonnet-5
- effort: low
- project: C:/Users/seamo/Documents/gemini/Jishushitsu
- created: 2026-08-28
- attempts: 0

## 1. ゴール
横断調査が本 repo について挙げた **1 件**の所見を実コードで検証し、
「採用（本当に不要）／却下（現役 or 誤検出）／保留（判断材料が足りない）」に仕分ける。

## 2. 背景（最小限）

2026-08-27〜28 に `C:/Users/seamo/Documents/gemini` 配下 26 repo / 3,119 スクリプトを横断調査した。
手法は「Claude が機械的な棚卸し（被参照数・エントリポイント・秘密情報スキャン）を生成 →
Antigravity CLI (gemini-3.7-flash-high, read-only) が判定 → Claude が機械照合して再判定」。

レポート正本: `../Botarhythm_Hub/docs/dead-script-sweep-20260827.md`（全所見は同名 .json）

**発注側は既に 2 つの誤りを出している。同じ轍を踏まないこと。**

1. **「即削除可」ラベルが楽観的すぎた**。被参照数ゼロを不要と読んだが、Next.js の
   `app/api/*/route.ts` は import されず URL で到達し、React コンポーネントや
   `package.json`・`.env.example`・テストも規約でロードされる。**静的な参照解析だけで
   削除判断をしてはいけない。**
2. **秘密情報の誤報**。`user-history` の `RAILWAY_TOKEN` を「実値が公開されている」と
   最重要所見に挙げたが、値は `${…}` 囲みのテンプレートで、対の project_id は Railway で
   404、起動対象スクリプトも存在しなかった＝一度も動いていない下書きだった。
   **値の形が本物らしいかだけでなく、参照先が実在するかまで見ること。**

したがって以下の所見一覧は**未検証の候補リスト**として扱い、採否は自分の手で取り直すこと。

## 3. スコープ

### 変更してよいファイル
- 本タスクファイル（検証結果の追記）

### 触ってはいけないもの（非スコープ）
- **実ファイルの削除・移動**（採否を決めるまで行わない。削除は別タスク＋オーナー承認）
- **push**（発注側のコミットも未 push。公開操作はオーナー判断）
- 鍵の発行・失効・ローテーションの実行（要否の判断までが本タスク）
- 他タスクの未コミット変更（本 repo には無関係な変更が残っている場合がある）

## 4. 制約
- 検証は read-only。判断が付かないものは無理に採用せず「保留」にする。
- **秘密情報の値そのものをタスクファイルに書かない。** 変数名・長さ・形状のみ記録する。
- 発注側の根拠を引用するだけの採用は不可。自分で確認したコマンドと結果を残すこと。

## 5. 受け入れ条件（AC）
- [ ] AC-1: `gh repo view --json visibility -q .visibility` → 可視性を記録する（PUBLIC なら以降の秘密情報の重大度が上がる）
- [ ] AC-2: `git log --all --diff-filter=A --name-only --format="" -- ".env*" "*_env.txt" "*.backup" "secrets/*"` → 秘密を含むファイルが過去に追加された履歴の有無を記録する
- [ ] AC-3: 下の所見一覧の全パスについて実在確認する。`ls <path>` が失敗するものは「却下（実在せず）」として表に記録する
- [ ] AC-4: 各所見について、規約ロードされる層でないかを判定する（フレームワークのルーティング／エントリポイント／設定／型定義／テスト／テンプレート）。該当するものは被参照ゼロでも「却下（現役）」とする
- [ ] AC-5: 残った候補それぞれについて、**自分で** grep を打ち直して被参照を確認し、採用／却下／保留を根拠つきで決める。発注側の根拠を引き写さないこと
- [ ] AC-6: 本ファイル末尾の「検証結果」に判定表と実行報告を追記し、status を done にする

## 6. 自己検証手順
1. AC を上から順に実行する。
2. 失敗したら修正して再実行（attempts +1）。attempts が 2 に達してなお失敗なら status: returned。
3. AC 自体が誤っていると判断したら AC を書き換えず `returned (reason: ac-dispute)` で返す。

## 7. 完了の定義（DoD）
- 全 AC 通過
- 「検証結果」に判定表（採用／却下／保留 と各根拠）を記入し status を done に更新
- 却下した所見は「なぜ発注側が誤ったか」を1行で書く（横断調査の手法改善に使う）

---

## 所見一覧（発注側の一次判定。**そのまま信じないこと**・全 1 件）

内訳: 1-即削除可 1件

| # | path | 分類 | 確信度 | git | 被参照 | 発注側の根拠（要検証） |
|---:|---|---|---|---|---:|---|
| 1 | `components/PresenceToast.tsx` | B-unreferenced | high | tracked | 0 | components/PresenceToast.tsx は参加者・退出者のトースト通知を表示するコンポーネント (export function PresenceToast) として定義されているが、プロジェクト内のどのファイルからも import / 参照されておらず (grep_search  |

---

## 検証結果（実行モデルが追記する）

- 結果: done / returned（reason: failed / ac-dispute / blocked）
- AC 結果: 
- 採用 / 却下 / 保留 の件数: 

| # | path | 判定 | 自分で確認した根拠（コマンドと結果） |
|---:|---|---|---|
| | | | |

- 発注側が誤った所見と、その原因: 
- 逸脱・気づき: 
