# AI 参加者 (ChatGPT デスクトップ音声) セットアップガイド — Windows

収録モードに「人間2名 + ChatGPT音声」の3者ポッドキャスト収録を追加する機能の、
ホストPC（Windows 11 + Chrome）での音声ルーティング手順。

アプリは OS レベルの音声しか扱わない。OpenAI API・ChatGPT の認証情報は一切使わない。

## 構成は2種類ある

| 構成 | 必要なケーブル | ChatGPT が聞ける声 | 用途 |
|---|---|---|---|
| **A. 簡易構成** | VB-CABLE **1本**（標準パッケージ） | ホスト（自分）のみ | まずはこれ。相手の発言はホストが要約して伝える |
| **B. フル構成** | VB-CABLE **2本**（A+B 拡張が必要） | ホスト + リモート参加者 | 3者が対等に会話する本番収録 |

まず A で動かし、必要になったら B へ上げるのが安全。

## 必要なもの（共通）

- ChatGPT デスクトップアプリ（音声モードが使えるプラン）
- [VB-Audio Virtual Cable](https://vb-audio.com/Cable/)
- **ヘッドホン必須**（スピーカーは自己ループの原因になるため禁止。特に構成 A では
  スピーカー使用が即座に「AIの声→スピーカー→物理マイク→ChatGPT入力」のループを作る）

---

# 事前確認: 音声設定なしで機能を試す

音声ルーティングを触る前に、機能そのものの動作を確認できる。

1. 収録モードに入り、StudioBar の 🤖 ボタンでセットアップを開く
2. 「AI 音声ソース」のプルダウンで選ぶ代わりに、ブラウザのコンソールで
   デバイスIDに `fake` を設定する（開発用フック）。または一時的にコード側で
   `sourceDeviceId: 'fake'` を渡す
3. 880Hz のトーンが2秒ごとに鳴る疑似 AI 参加者が接続され、AI タイルが
   speaking ⇄ idle を繰り返す

ChatGPT も VB-CABLE も不要。タイル表示・録画混入・再接続の確認はこれで足りる。

---

# 構成 A: VB-CABLE 1本（今すぐ使える）

```
ChatGPT [出力 = CABLE Input] ──→ CABLE Output ──→ 本アプリ「AI 音声ソース」
                                                    ├→ 録画に混入
                                                    ├→ リモート参加者へ配信
                                                    └→ 自分のヘッドホンへモニタ再生

ChatGPT [入力 = マイク配列(物理マイク)] ←── 自分の声だけが届く
```

## 手順

1. **ヘッドホンを接続する**（スピーカー禁止）。
2. ChatGPT デスクトップを起動し、**音声モードで一度何か喋らせる**
   （音を鳴らさないと次の音量ミキサーに項目が出ない）。
3. タスクバーのスピーカーアイコンを**右クリック → 音量ミキサーを開く**
   （または 設定 → システム → サウンド → 音量ミキサー）。
4. 一覧の **ChatGPT** の行を展開し、
   - **出力デバイス** = `CABLE Input (VB-Audio Virtual Cable)`
   - **入力デバイス** = `マイク配列 (Realtek(R) Audio)` ※物理マイク
5. この時点で **ChatGPT の声は直接は聞こえなくなる**（ケーブルへ流れるため）。
   これは正常。アプリで AI 参加者を有効化すると、アプリがヘッドホンへ再生する。
6. 自習室の収録モード → 🤖 ボタンでセットアップを開き、
   - **AI 音声ソース** = `★ CABLE Output (VB-Audio Virtual Cable)`
   - **ChatGPT への送出先** = `使用しない (外部でルーティング済み)`
   - 下部のチェックボックス「外部ルーティングで ChatGPT の声が ChatGPT
     自身の入力に戻らないことを確認しました」に**チェック**
7. ChatGPT に何か喋らせ、**レベルメーターが振れる**ことを確認。
8. 「AI 参加者を有効にする」を押す。

## 構成 A の制約

- **ChatGPT にはリモート参加者の声が届かない。** 相手が話した内容は、ホストが
  「今〇〇さんがこう言ったんだけど」と口頭で要約して ChatGPT に伝える運用になる。
- 録画・配信には3者すべての声が入る（この制約は「ChatGPT の耳」だけの話）。

---

# 構成 B: VB-CABLE 2本（フル3者会話）

構成 A の制約を外すには**2本目の仮想ケーブル**が要る。標準の VB-CABLE
パッケージには1本しか入っていないため、[VB-Audio](https://vb-audio.com/Cable/) の
**VB-Cable A+B for Windows**（追加ケーブル・寄付ウェア）を別途インストールする。
C+D は不要（さらに増やしたい場合の別パック）。

## インストール手順

1. VB-Audio のページで **「Donate for download VB-Cable A+B for Windows」** を押す。
   寄付ウェアなので金額を自分で決める画面を通ってからダウンロードリンクが出る
   （`VBCABLE_A_B_Driver_Pack45.zip`）。
2. zip を**展開してから** `VBCable_AB_PackSetup.exe` を**右クリック →
   管理者として実行**。zip の中身を直接実行するとドライバの導入に失敗する。
3. インストール後に **PC を再起動**（オーディオドライバのため必須）。
4. 再起動後、サウンドデバイス一覧に `CABLE-A Input/Output`・`CABLE-B Input/Output`
   が増えていることを確認する。既存の無印 `CABLE Input/Output` はそのまま残る
   （3本になるが、使うのは A と B）。

## 配線図

```
【ChatGPT の声を取り込む経路】
ChatGPT デスクトップ [出力デバイス = CABLE-A Input]
  → CABLE-A Output → 本アプリ「AI 音声ソース」で選択
       ├→ 録画ミキサー（自動）
       ├→ LiveKit 配信（リモート参加者が聴く・自動）
       └→ ホストのヘッドホンモニタ（自動）

【ChatGPT に聞かせる経路】
本アプリ「ChatGPT への送出先」= CABLE-B Input
  （アプリ内で「あなたのマイク + 相手の声」だけをミックスして送出。
   AI 自身の声は構造的に混ざらない）
  → CABLE-B Output = ChatGPT デスクトップ [入力デバイス]
```

## 設定手順

1. **ヘッドホンを接続する**（スピーカー禁止）。
2. Windows サウンド設定で **全デバイスを 48000 Hz に統一**
   （サンプルレート不一致は音割れ・ノイズの原因）。
   設定 → システム → サウンド → 各デバイス → 「形式」を 48000 Hz に。
3. ChatGPT デスクトップを起動し、**音声モードで一度喋らせる**（音を鳴らさないと
   次の音量ミキサーに項目が出ない）。
4. **音量ミキサー**（タスクバーのスピーカーアイコンを右クリック → 音量ミキサーを開く）で
   ChatGPT の行を展開し、
   - **出力デバイス** = `CABLE-A Input`
   - **入力デバイス** = `CABLE-B Output`

   ※ ChatGPT アプリ自体にデバイス選択が無くても、Windows の per-app 設定で確実に切り替わる。
5. 自習室の収録モードに入り、StudioBar の 🤖 ボタンからセットアップを開く。
6. 「AI 音声ソース」= **CABLE-A Output** を選択。ChatGPT に何か話させ、レベルメーターが
   反応することを確認。
7. 「ChatGPT への送出先」= **CABLE-B Input** を選択。
8. **プリフライト（自己ループ検査）を実行**。
   この検査中はまだ ChatGPT にこちらの声は届かないので、**ChatGPT にテキストで
   話しかけて音声で返答させながら**ボタンを押す。
   「AI の声が送出先に漏れていない」ことが確認されると合格。
   不合格の場合は配線（特に Windows の「このデバイスを聴く」設定が CABLE 間を
   繋いでいないか）を見直す。
9. 「AI 参加者を有効にする」を押す。ステージ下段に AI タイルが表示され、
   ChatGPT の発話でアバターが speaking 状態になる。
   **ここで初めて ChatGPT にこちらの声が届くようになる。**
10. **参加中のメンバーにページの再読み込みを依頼**（AI タイルは新しい画面でのみ表示される）。
11. 録画開始。**AI の有効化は必ず録画開始前に行う**（録画中の有効化は不可）。

---

# 動作確認チェックリスト（収録前 毎回）

| 確認 | 構成A の期待 | 構成B の期待 |
|---|---|---|
| 自分が話す | ChatGPT が反応する | ChatGPT が反応する |
| 相手が話す | （反応しない＝仕様） | ChatGPT が反応する |
| ChatGPT が話す | 自分のヘッドホンで聞こえる・相手にも聞こえる・AI タイルが speaking | 同左 |
| ChatGPT に「今の音を繰り返して」 | 自分の声だけ繰り返す（自己ループ無し） | 人間の声だけ繰り返す |

## トラブルシューティング

- **レベルメーターが動かない**: ChatGPT の出力デバイス設定がリセットされていないか確認
  （アプリ更新で既定デバイスに戻ることがある）。
- **「音声解析が停止中」表示**: 画面のどこかをクリックすると解除される（ブラウザの
  autoplay 制限）。
- **AI タイルが「⚠ 音声ソース切断」**: CABLE デバイスが無効化された。デバイスを戻して
  セットアップ画面の「再接続」を押す。**録画は途切れず継続している。**
- **遅延が大きい/音質が悪い**: 48kHz 統一を確認。改善しない場合は Voicemeeter Banana に
  よる低遅延ルーティング（上級者向け）を検討。
- **自分の声が二重に録れる**: 「AI 音声ソース」に物理マイクを選んでいないか確認
  （セットアップ画面に警告が出る）。
- **音量ミキサーに ChatGPT が出てこない**: そのアプリが音を鳴らしている間しか一覧に
  出ない。音声モードで喋らせた状態で開く。
- **ChatGPT が自分の声に反応し続ける（暴走）**: スピーカーを使っていないか確認。
  構成 A ではスピーカー使用が直ちに自己ループになる。

## 現在の環境メモ

このPC（2026-08-20 時点）は **標準 VB-CABLE 1本のみ**インストール済み
（`CABLE Input` / `CABLE Output`）。したがって現状は**構成 A** で動く。
構成 B にするには CABLE A+B の追加インストールが必要。
